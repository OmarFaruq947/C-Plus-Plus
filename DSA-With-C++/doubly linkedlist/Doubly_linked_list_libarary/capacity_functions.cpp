#include <bits/stdc++.h>
using namespace std;

int main()
{

    list<int> myList = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // cout << myList.max_size() << endl;

    // myList.clear();
    // myList.reverse();
    // myList.resize(3);
    // myList.assign(1, 10);

    for (int val : myList)
    {
        cout << val << endl;
    }

    return 0;
}